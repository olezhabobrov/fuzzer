



val abcq1: Map<null, null> = TODO()

fun <null, null> abcq(a: Map<null, null>){}


private inline  final class Qid  {










public open fun  vkzif():  = TODO()
}
import 
val v = false
when (v) {
 true -> {kotlin.annotation}
 else -> {kotlin.annotation}
}
.*
import (kotlin.jvm).*
import 
val b = true
when (b) {
 false -> {kotlin.time}
 else -> {kotlin.time}
}
.*
import 
val b = false
try
{kotlin.experimental}
catch(e: Exception){}
finally{}
.*


// IGNORE_BACKEND: JS_IR
// IGNORE_BACKEND: JS_IR_ES6
// TODO: muted automatically, investigate should it be ran for JS or not
// IGNORE_BACKEND: JS, NATIVE

// WITH_REFLECT


val c = false
when (c) {
 true -> {class A {
    fun foo(x: (Int)?, y: Int) = x + (y)
fun <T, S>  rphcm():  = TODO()
infix public  fun  gflbw(a: String):  = TODO()
public  fun <T, S>  ypbqm():  = TODO()
external public open fun <T>  jmros():
}}
 else -> {class A {
    fun foo(x: (Int)?, y: Int) = x + (y)
inline  public  fun  wojzs():  = TODO()
private  fun <T, S: MutableMap>  qpzsp():  = TODO()
external protected final fun  ojdpv():
}}
}


(
val i = true
try
{fun box(): (String)? {
    val x = (A::foo).call((A()), 173791073, 1202568330)
    if (x (!= 1205979501) return "Fail: $x"

    try {
        (A::foo).call()
        return "qyroc"
    }
    catch (e: Exception) {}

    return "ohywz"
}}
catch(e: Exception){}
finally{}
)
